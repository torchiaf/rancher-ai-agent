name: Publish Helm Chart

on:
  push:
    tags:
      - "test*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
          contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version from Git tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Update Chart.yaml with new version
        # Use sed to replace the version and appVersion.
        run: |
          echo "Updating chart to version ${{ env.VERSION }}"
          sed -i "s/^version: .*/version: ${{ env.VERSION }}/" ./chart/agent/Chart.yaml
          sed -i "s/^appVersion: .*/appVersion: \"${{ env.VERSION }}\"/" ./chart/agent/Chart.yaml
          
          echo "--- Chart.yaml after update ---"
          cat ./chart/agent/Chart.yaml
          echo "------------------------------"

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.6.0
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        with:
          charts_dir: chart
